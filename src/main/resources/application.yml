spring:
  thymeleaf:
    encoding: UTF-8
    cache: false # 开发时关闭缓存,不然没法看到实时页面
    mode: HTML
    servlet:
      content-type: text/html

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://10.111.24.205:3306/data-annotation?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: Analysis321!

mybatis:
  config-location: classpath:mybatis-config.xml
server:
  port: 8001

